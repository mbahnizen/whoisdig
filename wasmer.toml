[package]
name = "belajarnizen/whoisdig"
version = "0.2.0"
description = "Domain & DNS Intelligence Tool — WHOIS, RDAP, DNS Lookup"
license = "MIT"
private = true

[dependencies]
"php/php" = "*"

[fs]
"/app" = "."
