# Deny all access to storage directory
# This protects cache files, logs, and rate-limit data from public access

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
